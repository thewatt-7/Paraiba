[pytest]
markers =
    integration: marks tests as requiring real external services (deselect with -m "not integration")
    system: marks tests as system/end-to-end tests (full pipeline, all I/O mocked)
testpaths = tests

#This is initializes pytest and markers for it